/**
 * Defines a menu card.
 */
export interface MenuCards {
  uuid: string;
  /**
   * Reference to restaurant ID
   */
  restaurant: string;
  /**
   * Reference to the files
   */
  files: string[];
}
